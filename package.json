{
  "name": "bitpshr.net",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 🚀",
    "format": "npx prettier . --ignore-path .gitignore --write"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  },
  "prettier": {
    "useTabs": true,
    "overrides": [
      {
        "files": "package.json",
        "options": {
          "useTabs": false
        }
      }
    ]
  }
}
